# Act configuration for arch-devcontainer-features
# This file configures default settings for act (GitHub Actions runner)

# Use a compatible Ubuntu image for the GitHub Actions runner
# (this is NOT the image where features are tested - that's handled by devcontainer CLI)
--platform ubuntu-latest=catthehacker/ubuntu:act-latest

# Reuse containers between runs for faster execution
--reuse

# Enable verbose logging for better debugging
--verbose

# Set default environment variables
--env GITHUB_TOKEN=dummy_token_for_local_testing
